# AI Agent Habr Search Bot

MVP AI-агента для поиска и анализа статей с Хабра. Реализован в рамках проектной практики.

## Функциональность
- 📚 **Сбор данных**: автоматический парсинг свежих статей с Habr.com.
- 🔎 **Умный поиск**: поиск по смыслу (RAG) с использованием векторной базы ChromaDB.
- 🤖 **Telegram Бот**: удобный интерфейс для взаимодействия.
- ⚙️ **Фильтры**: возможность фильтрации результатов.
- 🔗 **Рекомендации**: поиск похожих статей.
- 🧠 **Тесты**: генерация проверочных вопросов по статье (Mock).

## Технический стек
- Python 3.10+
- aiogram 3.x (Telegram API)
- ChromaDB (Vector Store)
- SentenceTransformers (Embeddings)
- BeautifulSoup4 (Parsing)

## Установка и запуск

1. **Клонируйте репозиторий**
2. **Создайте виртуальное окружение**
   ```bash
   python -m venv venv
   source venv/bin/activate # Linux/Mac
   venv\Scripts\activate # Windows
   ```
3. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```
4. **Настройте переменные окружения**
   
   Создайте файл `.env` в корне проекта:
   ```env
   BOT_TOKEN=ваш_telegram_bot_token
   OPENAI_API_KEY=ваш_openai_api_key
   ```
   
   **Где получить токены:**
   - **BOT_TOKEN**: Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram командой `/newbot`
   - **OPENAI_API_KEY**: Получите на [platform.openai.com](https://platform.openai.com) в разделе API Keys (потребуется пополнить баланс ~$5)
   
5. **Запустите бота**
   ```bash
   python bot/main.py
   ```

## Архитектура
1. `core/scraper.py`: Загружает RSS ленту, парсит HTML страниц для получения полного текста.
2. `core/rag_engine.py`: Инициализирует ChromaDB, преобразует текст в векторы и сохраняет их. Обрабатывает поисковые запросы.
3. `bot/handlers.py`: Логика обработки сообщений и команд бота.
4. `bot/keyboards.py`: Интерфейсные элементы (кнопки).
